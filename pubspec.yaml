name: stuttgart_network
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # A safer SDK range

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8

  # --- ADDED DEPENDENCIES ---
  supabase_flutter: ^2.5.7  # Add this line for Supabase
  flutter_dotenv: ^5.1.0     # Add this line for .env keys
  image_picker: ^1.1.23  # Add this line for image picking
  permission_handler: ^10.4.0 # Add this line for handling permissions
  # --------------------------

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  # To add assets to your application, add an assets section, like this:
  assets:
    - lib/assets/.env # <--- ИСПРАВЛЕНО (появился отступ и дефис)